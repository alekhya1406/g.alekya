#include <stdio.h>

int main() {
    int x, y, result = 1, i = 1;
    
    printf("Enter the x & y values:\n");
    scanf("%d%d", &x, &y);
    
    while(i <= y) {
        result *= x;
        i++;
    }
    
    printf("\nResult: %d^%d = %d\n", x, y, result);
    return 0;
}
